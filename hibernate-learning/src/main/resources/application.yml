spring:
  datasource:
    username: sa
    password: pass
  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc.batch_size: 100
  h2:
    console:
      enabled: true
logging:
  level:
    liquibase: INFO
